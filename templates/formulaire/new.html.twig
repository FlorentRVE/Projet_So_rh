{% extends 'base.html.twig' %}

{% block title %}Nouveau Formulaire{% endblock %}

{% block body %}
    <div class="bg-sky-200 min-h-screen">

        <div class="flex flex-col p-4 justify-center items-center bg-slate-50">

            <h1 class="text-3xl font-semibold text-center">Créer un formulaire</h1>

        </div>

        {{ include('formulaire/_form.html.twig') }}

        <div class="flex justify-between px-4 w-[90%] mx-auto">
            <a href="{{ path('app_formulaire_index') }}" class="bg-sky-500 text-white p-2 rounded-lg mt-10">Retour</a>

        </div>
    </div>
{% endblock %}
