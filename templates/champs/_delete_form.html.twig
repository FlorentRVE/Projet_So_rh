<form method="post" action="{{ path('app_champs_delete', {'id': champ.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ champ.id) }}">
    <button class="bg-red-500 text-white p-2 rounded-lg">Supprimer</button>
</form>
